{
  "name": "traits",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ jiezhang/traits",
    "create-local": "graph create --node http://localhost:8020/ jiezhang/traits",
    "remove-local": "graph remove --node http://localhost:8020/ jiezhang/traits",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jiezhang/traits"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
